<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MeuApp"
        tools:targetApi="31">
        <activity
            android:name=".ListarProdutosActivity"
            android:exported="false" />
        <activity
            android:name=".ListaDoProduto"
            android:exported="false" />
        <activity
            android:name=".btnListarProdutos"
            android:exported="false" />
        <activity
            android:name=".Activities.ListarProdutosActivity"
            android:exported="false"
            android:label="Lista de produtos" />
        <activity
            android:name=".Activities.ProdutoActivity"
            android:exported="false"
            android:label="Cadastro de produtos" />
        <activity
            android:name=".Activities.VendaActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Cadastro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>